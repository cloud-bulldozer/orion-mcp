# secret-quay.yaml
apiVersion: v1
kind: Secret
metadata:
  name: quay-cred
  namespace: orion-mcp
  annotations:
    kubernetes.io/service-account.name: default
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: ${QUAY_CRED}
